name: Deploy to AWS S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{}}
        aws-secret-access-key: ${{ ohQBbDCsRtS4NrAO/u5lE9r3m4u/D2CbmlP/xcJJ }}
        aws-region: us-east-1

    - name: Sync S3 bucket
      run: |
        aws s3 sync . s3://dynamicdev-project --delete
